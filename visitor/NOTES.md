# Notes

- Language: C++17 canonical since 2026-09-22 (was Java-first, decided 2026-09-04). `visitor/code/` compiles; Java/C# snippets in lessons are transfer-only.
- Background: solid OOP + polymorphism — skip polymorphism primer, go straight to dispatch.
- Background: solid OOP + polymorphism — skip polymorphism primer, go straight to dispatch.
- Scope confirmed: core intent, accept/visit handshake, single vs double dispatch, tradeoffs.
- Mission Why still generic (extensible code, no instanceof chains). If user later names AST/compiler/interview goal, update MISSION.md + add learning record.
- Teaching prefs: short single-win lessons, equal-length quiz answers, Tufte-like clean type, print-friendly reference.
